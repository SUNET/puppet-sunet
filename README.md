puppet-sunet
===========

This branch is intentionally left empty in order to suppress warnings from machines that clone this repo before we changed default branch to testing.

E.g

```
70run-post-tasks: invoking /var/cache/cosmos/model/post-tasks.d/018packages
Your configuration specifies to merge with the ref 'refs/heads/main'
from the remote, but no such ref was fetched.
```


