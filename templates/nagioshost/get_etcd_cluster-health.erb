#!/bin/sh
#
# Execute "/etcdctl cluster-health" within the container etcd_sunetweb
# to check if the etcd cluster is healthy or not.
# This is a hack to allow Nagios to do exactly this operation from the
# NRPE check etcd-cluster-health, using sudo.
#

exec /usr/bin/docker exec etcd_sunetweb /etcdctl cluster-health
