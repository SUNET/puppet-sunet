#!/bin/sh
#
# Inspect a docker container and output the bridge name (if any).
# This is a hack to allow Nagios to do exactly this operation from the
# NRPE check check_docker_hostnames, using sudo.
#

exec /usr/bin/docker inspect --format '{{ .NetworkSettings.Bridge }}' "$1"
