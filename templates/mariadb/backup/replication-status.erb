#!/bin/bash

if docker exec -t mariadb-db-1 sh -c 'command -v mariadb' &>/dev/null; then
    CMD="mariadb"
else
    CMD="mysql"
fi

docker exec mariadb-db-1 "${CMD}" -u root -p'<%= @mariadb_root_password %>' -N -B -e "show status like 'Slave_running'"
