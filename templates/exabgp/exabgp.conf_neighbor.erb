
   neighbor <%= @peer_address %> {
        router-id <%= @router_id_config %>;
        local-as <%= @local_as %>;
        local-address <%= @local_address %>;
        peer-as <%= @peer_as %>;
        group-updates;

        family {
                ipv4 unicast;
        }

   }
