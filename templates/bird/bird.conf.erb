# Do not edit by hand - maintained by puppet

log syslog all;
router id <%= @my_router_id %>;
include "/etc/bird/conf.d/*.conf";

table internal;
protocol device {
   scan time 10;
   table internal;
}