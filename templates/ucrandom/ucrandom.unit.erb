[Unit]
Description=Feed the random pool

[Service]
EnvironmentFile=-/etc/default/ucrandom
ExecStart=/usr/bin/ucrandom -d ${UCRANDOM_DEVICE} -p ${UCRANDOM_PORT} ${UCRANDOM_SERVER}
IgnoreSIGPIPE=false
KillMode=process

[Install]
WantedBy=multi-user.target
