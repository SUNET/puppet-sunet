#!/usr/bin/env bash

# Make sure that naemon has time to write down retention.dat
/usr/bin/docker exec <%= @naemon_container %> bash -c 'pkill -o naemon'

/usr/bin/docker compose -f /opt/naemon_monitor/docker-compose.yml down
