---
services:
  patroni:
    container_name: patroni
    network_mode: host
    image: docker.sunet.se/patroni:<%= @patroni_imagetag %>
    volumes:
      - '/etc/ssl:/etc/ssl:ro'
      - '/opt/patroni/config/patroni.yml:/home/postgres/patroni.yml:ro'
      - '/opt/patroni/certs:/opt/patroni/certs:ro'
<% if @legacy_ca == true %>
      - '/etc/ssl/certs/infra.crt:/etc/ssl/certs/infra.crt:ro'
<% end %>
      - postgres_data:/var/lib/postgresql
      <% if @pgbackrest == true %>
      - '/usr/bin/pgbackrest:/usr/bin/pgbackrest'
      - '/opt/patroni/config/pgbackrest.conf:/etc/pgbackrest.conf:ro'
      - '/root/.ssh/id_ed25519:/home/postgres/.ssh/id_ed25519:ro'
      - '/etc/ssh/ssh_known_hosts:/etc/ssh/ssh_known_hosts:ro'
      <% end %>

    entrypoint: /usr/bin/python3
    command: /patroni.py patroni.yml

volumes:
  postgres_data:
